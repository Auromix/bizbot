# We-Business-Manager 配置文件
# 复制此文件为 .env 并填入真实值，或运行 python scripts/setup_env.py 交互式生成
#
# 也可以直接设置环境变量（环境变量优先级高于 .env 文件）

# === MiniMax LLM 配置（必填） ===
MINIMAX_API_KEY=your-minimax-api-key-here
MINIMAX_MODEL=MiniMax-M2.5
MINIMAX_BASE_URL=https://api.minimaxi.com/anthropic

# === 数据库配置 ===
DATABASE_URL=sqlite:///data/store.db

# === Bot 配置 ===
BOT_NAME=小助手
TARGET_GROUP_NAME=门店经营群

# === Web 平台配置 ===
WEB_HOST=0.0.0.0
WEB_PORT=8080
WEB_USERNAME=admin
WEB_PASSWORD=admin123
WEB_SECRET_KEY=change-me-to-a-random-secret-key

# === 语音转写配置（可选，用于处理语音消息） ===
# VOICE_TRANSCRIBE_API_KEY=
# VOICE_TRANSCRIBE_API_BASE=
VOICE_TRANSCRIBE_MODEL=whisper-1
VOICE_TRANSCRIBE_LANGUAGE=zh

# === 其他配置 ===
CONFIDENCE_THRESHOLD=0.7
DAILY_SUMMARY_TIME=21:00
